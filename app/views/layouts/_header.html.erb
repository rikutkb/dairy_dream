<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to "夢日記",   root_url(protocol: 'http') %>
    <form class="d-flex">
      <button class="btn btn-outline-success me-2" type="button"><%= link_to "Home",root_path(protocol: 'http') %></button>
      <% if logged_in? %>
      <button class="btn btn-outline-success me-2" type="button"><%= link_to "Log out",logout_path(protocol: 'http'),method: :delete %></button>
      <button class="btn btn-outline-success me-2" type="button"><%= link_to "User", user_path(@current_user,protocol: 'http') %></button>


      <% else %>
        <button class="btn btn-outline-success me-2" type="button"><%= link_to "Log in", login_path(protocol: 'http') %></button>
        <button class="btn btn-outline-success me-2" type="button"><%= link_to "Sign Up", signup_path(protocol: 'http') %></button>


      <% end %>
    </form>
  </div>
</nav>
