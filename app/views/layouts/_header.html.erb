<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to "夢日記",   root_url(protocol: 'https') %>
    <form class="d-flex">
      <button class="btn btn-outline-success me-2" type="button"><%= link_to "Home",root_path(protocol: 'https') %></button>
      <% if logged_in? %>
      <button class="btn btn-outline-success me-2" type="button"><%= link_to "Log out",logout_path(protocol: 'https'),method: :delete %></button>
      <button class="btn btn-outline-success me-2" type="button"><%= link_to "User", user_path(@current_user,protocol: 'https') %></button>


      <% else %>
        <button class="btn btn-outline-success me-2" type="button"><%= link_to "Log in", login_path(protocol: 'https') %></button>
        <button class="btn btn-outline-success me-2" type="button"><%= link_to "Sign Up", signup_path(protocol: 'https') %></button>


      <% end %>
    </form>
  </div>
</nav>
