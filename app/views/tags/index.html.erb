<% provide(:title, 'Tag一覧') %>

<h1>Tag一覧</h1>
<%= form_with url: tags_path, method: :get, local: true do |f| %>
    タグ名:
    <%= f.text_field :tag, value: @query %>
    <%= submit_tag 'search' %>

<% end %>
場所:<br>
<%= render partial:'tags',locals:{tags:@tags,kind:0} %>

人物:<br>
<%= render partial:'tags',locals:{tags:@tags,kind:1} %>
