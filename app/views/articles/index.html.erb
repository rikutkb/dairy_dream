<% provide(:title, '日記一覧') %>

<h1>Diary Dream App</h1>
<% @articles.each do |article| %>
    <ul>
        <li>
            <%= article.content %>
        </li>
    </ul>
<% end %>
<%= link_to '新規投稿', '/articles/new' %>
