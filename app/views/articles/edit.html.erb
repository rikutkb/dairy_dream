<% provide(:title, '日記作成') %>

<% if logged_in? %>
  <div class="row">
    <section class="user_info">
      <%= render 'shared/user_info' %>
    </section>
    <section class="article_form">
      <%= render 'shared/article_form' %>
    </section>
  </div>
<% else %>

<% end %>
